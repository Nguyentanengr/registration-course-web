import { OpenSectionContainer } from './OpenSection.styled';

const OpenSection = () => {
    return (
        <OpenSectionContainer>
            
        </OpenSectionContainer>
    );
};

export default OpenSection;